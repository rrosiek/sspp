{
  "scripts": {
    "emulate": "dotenv -c -- firebase use --project demo-sspp && firebase emulators:start --export-on-exit=./seed --import=./seed"
  },
  "devDependencies": {
    "dotenv-cli": "^5.1.0",
    "firebase": "^9.6.10"
  }
}
